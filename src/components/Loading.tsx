import React from "react";
import { SafeAreaView, Text, View } from "react-native";
export const Loading = () => {
  return (
    <SafeAreaView>
      <View>
        <Text>Loading...</Text>
      </View>
    </SafeAreaView>
  )
}