import { StyleSheet } from "react-native";
const styles = StyleSheet.create({
    container: {
        height: 700,
        width: 350,
        alignItems: "center",
        marginTop: 25
    },
    button: {
        marginTop: 25,
        marginBottom: 25
    }
})

export default styles;